#ifndef _STRING_PENDU_H_
#define _STRING_PENDU_H_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

// Prototype des fonctions
void chaine_pop(char chaine[], int mode);
int verifie_lettre(char mot[], char recherche[], char c);
void str_toupper(char str[]);

#endif